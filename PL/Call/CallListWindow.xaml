<Window x:Class="PL.Call.CallListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        mc:Ignorable="d"
        Title="CallListWindow" Height="640" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        FlowDirection="RightToLeft">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid HorizontalAlignment="Stretch" Height="auto" Grid.Row="0" VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
        <StackPanel Grid.Column="0">
            <Label  Content=" סינון לפי סוג קריאה:"/>
                <ComboBox SelectedValue="{Binding CallType, Mode=TwoWay}"
                  ItemsSource="{Binding Source= {StaticResource CallTypeCollectionKey}}"
                  SelectionChanged="filterBySelect_SelectionChanged"
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1">
                <Label  Content="מיין לפי:"/>
                <ComboBox SelectedValue="{Binding Attribute, Mode=TwoWay}"
           ItemsSource="{Binding Source= {StaticResource CallInListAttributesCollectionKey}}"
           SelectionChanged="filterBySelect_SelectionChanged"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </StackPanel>

            <StackPanel Grid.Column="2" >
                <Label  Content="סינון לפי סטטוס קריאה"/>
                <ComboBox SelectedValue="{Binding Status, Mode=TwoWay}"
                ItemsSource="{Binding Source={StaticResource StatusCallCollectionKey}}"
                SelectionChanged="filterListByStatus"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </StackPanel>
        </Grid>
        
        <DataGrid Grid.Row="1" ItemsSource="{Binding Path=CallList}" IsReadOnly ="True" AutoGenerateColumns="False"
     EnableRowVirtualization="True" RowDetailsVisibilityMode="VisibleWhenSelected"
     MouseDoubleClick="callsList_MouseDoubleClick" SelectedItem="{Binding SelectedCall}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Delete" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🚮" Click="delete_btnClick" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="undo assign" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="❌" Click="CancelTreatCall_Click" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Binding="{Binding CallId}" Header="CallId" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding CallType}" Header="Call Type" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Background" 
                                Value="{Binding CallType, Converter={StaticResource ConverterEnumCallTypeToColorKey}}" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="OpeningTime" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding OpeningTime, StringFormat='{}{0:HH:mm dd/MM/yyyy}'}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Total Time Remaining Finish Calling" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TotalTimeRemainingFinishCalling, StringFormat='{}{0:HH:mm dd/MM/yyyy}'}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


                <DataGridTextColumn Binding="{Binding LastVolunteerName}" Header="Last Volunteer Name" Width="Auto"/>
                <DataGridTemplateColumn Header="Total Time Complete Treatment" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TotalTimeCompleteTreatment, StringFormat='{}{0:HH:mm dd/MM/yyyy}'}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Binding="{Binding TotalCallsCanceledByVolunteer}" Header="Total Calls Canceled By Volunteer" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding StatusCall}" Header="Status Call" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Background" 
                                Value="{Binding StatusCall, Converter={StaticResource ConverterEnumStatusCallToColorKey}}" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding TotalAssignments}" Header="Total Assignments" Width="Auto"/>

            </DataGrid.Columns>
        </DataGrid>        <Button Content="Add" Grid.Row="2" HorizontalAlignment="Center" Height="50" Width="100" Click="Button_Click"/>
    </Grid>
</Window>